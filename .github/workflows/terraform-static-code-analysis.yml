name: "Terraform: Static Analysis"

on:
  pull_request:
    paths:
      - 'terraform/**'
      - '.github/workflows/terraform-static-code-analysis.yml'
    branches:
      - master
    types: [opened, edited, reopened, synchronize]
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  run-tf-analysis:
    name: TFSEC & CHECKOV Static Analysis
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
      - name: Scan
        uses: davidkelliott/terraform-static-analysis@main
        with:
          terraform_working_dir: terraform
          scan_type: full

  manual-trigger-only:
    name: TFSEC & CHECKOV Static Analysis - FULL
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
      - name: Scan
        uses: davidkelliott/terraform-static-analysis@main
        with:
          scan_type: full
          checkov_exclude: CKV_AWS_8,CKV_AWS_135

# git diff --no-commit-id --name-only -r @^ | awk '{print $1}' | grep '.tf' | sed 's#/[^/]*$##' | uniq